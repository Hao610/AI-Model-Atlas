← 返回 [深潜专题目录](../DEEP_DIVES_zh.md) | [[English] (01_ai_intelligence.md)](01_ai_intelligence.md) | [中文]

---

# 01. 为什么 AI 会变聪明？
> **从规则系统到 GPT 的 70 年计算智能进化史。**

在计算机科学发展的早期，构建人工智能（AI）主要依靠人工编写的**“规则”**。如果一个医生要诊断疾病，程序员就得写下几千行 `if-then`（如果……那么……）语句，把症状和诊断结果一一映射。这类系统被称为**专家系统 (Expert System)**——它们脆弱、无法自我学习，一旦遇到编写规则之外的情况就会瞬间崩溃。

真正的范式跃迁发生在我们停止编写规则，转而让计算机去“阅读海量数据，自己寻找规律”的时刻。

#### 1. 手工特征时代 (Feature Engineering)
在深度学习爆发之前，机器学习工程师 90% 的精力都花在**特征工程 (Feature Engineering)**上——即通过人工编写代码，从原始数据中提取出关键的特征，再喂给模型。
* **垃圾邮件分类案例**：在 2005 年，如果工程师想识别垃圾邮件，他们需要手工设计各种规则：统计感叹号的数量、检测“特惠/发票”等关键词的出现频率、计算大写字母占邮件长度的比例。
* **瓶颈所在**：模型的智能上限完全取决于人类手工设计特征的能力。如果垃圾邮件发送者稍微改变拼写（如将“发票”写成“发.票”），人工设计的特征就会失效，工程师必须重新写代码来提取特征。

#### 2. 古典机器学习时代 (Classical ML) —— 找规律的算盘
* **随机森林 (Random Forest)**：传统的单颗决策树极易死记硬背数据（过拟合）。随机森林通过训练一大群互不相同的决策树，并在最终预测时让它们投票决定，从而大大提升了泛化能力。时至今日，它依然是处理结构化表格数据（如预测房价、银行风控、信用卡欺诈等）的王者。
* **XGBoost (极限梯度提升)**：一种极其高效的渐进式决策树提升系统。它采用串行方式构建决策树，让每一颗新树都专门去纠正上一颗树犯下的错误，是各大算法建模大赛的夺冠常客。

#### 3. 深度学习觉醒时代 (Deep Learning) —— 模拟大脑的神经网络
* **多层感知机 (MLP)**：现代深度学习的鼻祖。它通过层层相连、模拟人类神经元连接的数学加权计算，实现了复杂非线性函数的拟合。
* **卷积神经网络 (CNN)**：通过在图片上滑动微小的数学滤波器（卷积核）来提取边缘、形状和物体特征，这模仿了人类视觉皮层的工作原理，彻底改写了计算机视觉的历史。

深度学习彻底终结了手工特征的瓶颈。模型不再依赖人类灌输“有什么特征”，而是通过多层神经网络直接读取原始数据（如像素、文本 Token），在大脑深处自动学习和构建多层级的抽象特征表达。

---

既然你已经了解了 AI 是如何变聪明的，接下来让我们在 [Transformer 为什么统治了一切？](02_transformer.md) 中探索支撑现代大模型的架构。
